<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Use NoActionBar theme so we can host our Toolbar -->
    <style name="Theme.MRSummariesApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/md_theme_primary</item>
        <item name="colorOnPrimary">@color/md_theme_onPrimary</item>
        <item name="colorSecondary">@color/md_theme_secondary</item>
        <item name="android:statusBarColor">@color/md_theme_primary</item>
        <item name="android:windowBackground">@color/background</item>

        <!-- Floating action button colors -->
        <item name="materialButtonStyle">@style/Widget.MRSummariesApp.Button</item>
    </style>

    <style name="Widget.MRSummariesApp.Button" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/accent_1</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textColor">@color/md_theme_onPrimary</item>
    </style>
</resources>