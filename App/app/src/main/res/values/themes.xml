<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- App theme (Material Components parent) -->
    <style name="Theme.MrSummaries_App" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Core colors (adjust to your palette) -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <item name="colorBackground">@color/white</item>
        <item name="colorOnBackground">@color/black</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>

        <!-- window background and status bar -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- common attributes that views request -->
        <item name="selectableItemBackground">?attr/selectableItemBackground</item>
        <item name="android:buttonStyle">@style/Widget.MaterialComponents.Button</item>

        <!-- If using Card/Material components -->
        <item name="materialCardViewStyle">@style/Widget.MaterialComponents.CardView</item>
    </style>

    <!-- Provide a night variant if needed -->
</resources>