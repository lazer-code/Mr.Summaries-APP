<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Declare theme color attributes used by view-based themes/layouts -->
    <attr name="colorBackground" format="color" />
    <attr name="colorOnBackground" format="color" />
    <attr name="colorSurface" format="color" />
    <attr name="colorOnSurface" format="color" />
    <!-- common Material attributes (declaring these is optional if parent theme already exposes them, but safe to include) -->
    <attr name="colorPrimary" format="color" />
    <attr name="colorOnPrimary" format="color" />
    <attr name="colorControlHighlight" format="color" />
</resources>